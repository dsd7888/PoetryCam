{
  "version": 2,
  "builds": [
    { "src": "api/generate-poetry.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/generate-poetry.js" },
    { "src": "/(.*)", "dest": "/public/$1" }
  ]
}