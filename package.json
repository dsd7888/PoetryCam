{
  "name": "poetry_cam",
  "version": "1.0.0",
  "description": "A web application that generates AI poetry based on images from camera or uploads",
  "main": "server.js",
  "engines": {
    "node": ">=14"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "mkdir -p .vercel/output && cp -r public .vercel/output/static",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@google/generative-ai": "^0.24.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "multer": "^1.4.5-lts.2"
  }
}
